import React from 'react';

export interface ItemProps {
    /**
     * children
     * @type {React.ReactNode}
     */
    children: React.ReactNode;
    /**
     * active
     * @type {boolean}
     */
    active: boolean;
}
